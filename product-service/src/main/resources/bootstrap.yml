server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  application:
    name: product-service
  profiles:
    active: dev

  cloud:
    config:
      name: product-service
      uri: http://localhost:8888
      server:
        bootstrap: true





